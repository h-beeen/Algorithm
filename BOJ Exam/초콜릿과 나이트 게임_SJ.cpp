#include <iostream>
#include <queue>
using namespace std;

int x, y;
int dx[4] = {-1, 0 ,1, 0};
int dy[4] = {0, 1, 0, -1};

int	main (void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	cin >> x >> y;
	
}
