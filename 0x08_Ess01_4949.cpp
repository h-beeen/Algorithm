#include <iostream>
#include <string>
#include <stack>

using namespace std;

int main (void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	while(1)
	{
		int isValid = 1;
		string a;

		getline(cin, a);
		if(a == ".")
			break;

		stack <char> st;
		
		cout << a; 
	}
}
